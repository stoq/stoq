widget: CalculatorPopup
stoq+lib+gui+widgets+calculator+CalculatorPopup(): title=None, hidden
  GtkFrame():
    GtkAlignment():
      GtkBox(main_box, orientation=vertical): hidden
        GtkBox(main_widget, orientation=vertical, fill=True):
          GtkLabel(_main_label, expand=True, fill=True):
          ProxyEntry(_entry, expand=True, fill=True): ''
          GtkBox(orientation=horizontal, expand=True, fill=True):
            GtkLabel(_label, expand=True, fill=True):
            GtkImage(_warning): hidden
