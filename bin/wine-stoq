#!/bin/sh
export PYTHONPATH=`pwd`/../stoqlib\;`pwd`/../kiwi\;`pwd`/../stoqdrivers
export WINEDEBUG=fixme-all
export WINEPREFIX=`cat .wine-build-stoq-prefix`
# To avoid dragging in canberra
unset GTK_MODULES

STOQ=$(dirname `which stoq`)/stoq
wine c:/Python26/pythonw.exe $STOQ $@
